"use client"; import {FormEvent,useState} from "react"; import {useCart} from "@/components/CartProvider"; export default function Checkout(){const {items,total,setItems}=useCart();const [msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const payload={customer_name:f.get("name"),email:f.get("email"),phone:f.get("phone"),order_type:f.get("type"),items,total};const r=await fetch("/api/orders",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const d=await r.json();setMsg(d.message||"Order created");if(r.ok)setItems([])}return <main className="container" style={{padding:"60px 22px",maxWidth:760}}><h1 style={{fontSize:46}}>Checkout</h1><form onSubmit={submit} className="card" style={{padding:25}}><label>Full name</label><input name="name" required/><label>Email</label><input name="email" type="email" required/><label>Phone</label><input name="phone" required/><label>Order type</label><select name="type"><option>Pickup</option><option>Delivery</option></select><p style={{fontSize:22}}>Total: <b style={{color:"#d8a84e"}}>€{total.toFixed(2)}</b></p><button className="btn btn-gold" style={{width:"100%"}}>Place order</button>{msg&&<p style={{color:"#8ee59b"}}>{msg}</p>}</form></main>}