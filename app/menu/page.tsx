"use client"; import {useState} from "react"; import {menu} from "@/lib/menu"; import FoodCard from "@/components/FoodCard"; export default function Menu(){const cats=["All",...Array.from(new Set(menu.map(x=>x.category)))];const [cat,setCat]=useState("All");const list=cat==="All"?menu:menu.filter(x=>x.category===cat);return <main className="container" style={{padding:"70px 22px"}}><span style={{color:"#d8a84e"}}>DISCOVER</span><h1 style={{fontSize:52,marginTop:8}}>Our Menu</h1><div style={{display:"flex",gap:10,flexWrap:"wrap",margin:"25px 0 35px"}}>{cats.map(c=><button key={c} onClick={()=>setCat(c)} className={"btn "+(cat===c?"btn-gold":"btn-dark")}>{c}</button>)}</div><div className="grid-food">{list.map(x=><FoodCard key={x.id} food={x}/>)}</div></main>}