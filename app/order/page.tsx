"use client"; import {useCart} from "@/components/CartProvider"; import Link from "next/link"; export default function Order(){const {items,remove,total}=useCart();return <main className="container" style={{padding:"60px 22px"}}><h1 style={{fontSize:48}}>Your Order</h1>{!items.length?<div className="card" style={{padding:30}}>Your cart is empty. <Link href="/menu" style={{color:"#d8a84e"}}>Browse menu →</Link></div>:<><div className="card" style={{padding:20}}>{items.map((x:any)=><div key={x.id} style={{display:"flex",justifyContent:"space-between",padding:"16px 0",borderBottom:"1px solid #29292d",gap:12}}><div><b>{x.name}</b><div style={{color:"#999"}}>€{x.price} × {x.qty}</div></div><div><b>€{(x.price*x.qty).toFixed(2)}</b><button onClick={()=>remove(x.id)} style={{display:"block",marginTop:6,background:"none",border:0,color:"#e77"}}>Remove</button></div></div>)}</div><div style={{display:"flex",justifyContent:"space-between",margin:"24px 0",fontSize:24}}><b>Total</b><b style={{color:"#d8a84e"}}>€{total.toFixed(2)}</b></div><Link href="/checkout" className="btn btn-gold">Continue to checkout</Link></>}</main>}