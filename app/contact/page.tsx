"use client"; import {FormEvent,useState} from "react"; export default function Contact(){const [msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f.entries()))});const d=await r.json();setMsg(d.message)}return <main className="container" style={{padding:"60px 22px",maxWidth:760}}><h1 style={{fontSize:48}}>Contact us</h1><form onSubmit={submit} className="card" style={{padding:25}}><label>Name</label><input name="name" required/><label>Email</label><input name="email" type="email" required/><label>Message</label><textarea name="message" required rows={6}/><button className="btn btn-gold" style={{width:"100%",marginTop:18}}>Send message</button>{msg&&<p>{msg}</p>}</form></main>}