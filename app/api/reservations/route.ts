import {NextResponse} from "next/server"; export async function POST(req:Request){const body=await req.json();if(!body.customer_name||!body.email||!body.reservation_date)return NextResponse.json({message:"Invalid reservation data"},{status:400});return NextResponse.json({ok:true,reservation_id:"RES-"+Date.now(),message:"Reservation received successfully. Connect Supabase for production storage."})}