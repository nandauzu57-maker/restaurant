import {NextResponse} from "next/server"; export async function POST(req:Request){const body=await req.json();if(!body.customer_name||!body.email||!Array.isArray(body.items)||!body.items.length)return NextResponse.json({message:"Invalid order data"},{status:400});return NextResponse.json({ok:true,order_id:"ORD-"+Date.now(),message:"Order received successfully. Connect Supabase/Stripe for production payment flow."})}